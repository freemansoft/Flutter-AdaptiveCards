// =============================================================================
// Stub classes for Widgetbook navigation (matching route from example app)
// These are used as `type` in @widgetbook.UseCase to group use cases
// =============================================================================

import 'package:flutter/material.dart';

/// Stub for 'Samples' route
class const Examples({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'TextBlock' route
class const TextBlock({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Image' route
class const Image({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Container' route
class const Container({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ColumnSet' route
class const ColumnSet({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Column' route
class const Column({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'FactSet' route
class const FactSet({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ImageSet' route
class const ImageSet({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ActionSet' route
class const ActionSet({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Text' route
class const InputText({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Number' route
class const InputNumber({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Media' route
class const Media({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Date' route
class const InputDate({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Time' route
class const InputTime({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Toggle' route
class const InputToggle({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.ChoiceSet' route
class const InputChoiceSet({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Table' route
class const Table({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Badge' route (v1.6)
class const Badge({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Icon' route (v1.5 hub)
class const Icon({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Rating' route (v1.6)
class const Rating({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Carousel' route (v1.6)
class const Carousel({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Accordion' route (v1.6)
class const Accordion({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'CompoundButton' route (v1.6 hub)
class const CompoundButton({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Charts' Char.Donut, Char.Bar, Char.Line route (v1.6)
class const Charts({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Actions' route (v1.6)
class const Actions({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Actions' route (v1.6)
class const AdaptiveCardElement({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'CodeBlock' route (v1.6)
class const CodeBlock({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ProgressBar' route (v1.6)
class const ProgressBar({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ProgressRing' route (v1.6)
class const ProgressRing({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for Network Samples
class const Microsoft15({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for Github pnp Samples
class const GitHubPNPSamples({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Form via Assets' and 'Form with initData' routes
class const Forms({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for responsive-layout (`targetWidth` / `Layout.Flow`) demos
class const Responsive({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for the Teams `roundedCorners` demo (Container, ColumnSet, Column,
/// Table, Image)
class const RoundedCorners({super.key}) extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
