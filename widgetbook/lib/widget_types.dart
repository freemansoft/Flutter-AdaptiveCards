// =============================================================================
// Stub classes for Widgetbook navigation (matching route from example app)
// These are used as `type` in @widgetbook.UseCase to group use cases
// =============================================================================

import 'package:flutter/material.dart';

/// Stub for 'Samples' route
class Examples extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'TextBlock' route
class TextBlock extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Image' route
class Image extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Container' route
class Container extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ColumnSet' route
class ColumnSet extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Column' route
class Column extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'FactSet' route
class FactSet extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ImageSet' route
class ImageSet extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ActionSet' route
class ActionSet extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Text' route
class InputText extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Number' route
class InputNumber extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Media' route
class Media extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Date' route
class InputDate extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Time' route
class InputTime extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.Toggle' route
class InputToggle extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Input.ChoiceSet' route
class InputChoiceSet extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Table' route
class Table extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Badge' route (v1.6)
class Badge extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Icon' route (v1.5 hub)
class Icon extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Rating' route (v1.6)
class Rating extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Carousel' route (v1.6)
class Carousel extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Accordion' route (v1.6)
class Accordion extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'CompoundButton' route (v1.6 hub)
class CompoundButton extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Charts' Char.Donut, Char.Bar, Char.Line route (v1.6)
class Charts extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Actions' route (v1.6)
class Actions extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Actions' route (v1.6)
class AdaptiveCardElement extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'CodeBlock' route (v1.6)
class CodeBlock extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ProgressBar' route (v1.6)
class ProgressBar extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'ProgressRing' route (v1.6)
class ProgressRing extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for Network Samples
class Microsoft15 extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for Github pnp Samples
class GitHubPNPSamples extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for 'Form via Assets' and 'Form with initData' routes
class Forms extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for responsive-layout (`targetWidth` / `Layout.Flow`) demos
class Responsive extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

/// Stub for the Teams `roundedCorners` demo (Container, ColumnSet, Column,
/// Table, Image)
class RoundedCorners extends StatelessWidget {
  const new({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
