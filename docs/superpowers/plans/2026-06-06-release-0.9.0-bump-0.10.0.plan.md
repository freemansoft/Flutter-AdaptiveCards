# Release 0.9.0 and Prep 0.10.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Status:** **Historical — completed.** **0.9.0** published and tagged (`v0.9.0`, package tags); monorepo bumped to **0.10.0** for next cycle. Retained as release-engineer reference only; do not execute again.

**Goal:** Ship the monorepo at **0.9.0** to pub.dev (three library packages), tag the repo, then bump the entire tree to **0.10.0** for the next development cycle.

**Architecture:** Follow the monorepo **release-engineer** protocol: shared version across five `pubspec.yaml` files, five `CHANGELOG.md` files, publish only the three library packages in dependency order, tag before publish, post-release minor bump only after pub.dev confirms all packages are live.

**Tech Stack:** Flutter/Dart monorepo (fvm), pub.dev publish, git tags, GitHub Actions CI (`test.yml`).

---

## Current state (completed — audit 2026-06-08)

| Item                                     | Status                                                        |
| ---------------------------------------- | ------------------------------------------------------------- |
| **0.9.0** release                        | Published; tags `v0.9.0`, `vflutter_adaptive_*_fs-0.9.0`      |
| Monorepo dev version                     | **0.10.0** (all five `pubspec.yaml` files)                    |
| `flutter_adaptive_charts_fs` in-repo dep | `flutter_adaptive_cards_fs: ^0.10.0` (current cycle)          |
| CHANGELOG structure                      | 0.9.0 notes finalized; 0.10.0 section holds post-release work |

**Version targets:**

| Phase        | Published / dev version | Tags                                            |
| ------------ | ----------------------- | ----------------------------------------------- |
| Release      | `0.9.0`                 | `v0.9.0`, `vflutter_adaptive_cards_fs-0.9.0`, … |
| Post-release | `0.10.0`                | (no tag until next release)                     |

---

## Phase A — Finalize 0.9.0 release notes

### Task 1: Fix `flutter_adaptive_cards_fs` CHANGELOG

**Files:**

- Modify: `packages/flutter_adaptive_cards_fs/CHANGELOG.md`

The `## [0.10.0]` block (lines 8–28) documents work done **during** the 0.9.0 cycle. Merge it into `## [0.9.0]` and delete the premature 0.10.0 section.

- [x] **Step 1: Replace the top of the file through `## [0.9.0]`**

Replace lines 8–33 with:

```markdown
## [0.9.0]

### Added

- Filtered `Input.ChoiceSet` modal lists and typeahead search; choice **titles** drive search while submit, `onChange`, and `Data.Query` use choice **values**.
- Dependent `Input.ChoiceSet` support via `valueChangedAction` and host-driven `applyUpdates` / `Data.Query` cascade.
- **`OpenUrlActionInvoke`**, **`OpenUrlDialogActionInvoke`**, and **`InputChangeInvoke`** public models exported from `flutter_adaptive_cards_fs.dart`.
- Tests: `test/actions/open_url_action_invoke_test.dart`, `test/actions/open_url_dialog_action_invoke_test.dart`.

### Changed

- **`onSubmit`** now receives **`SubmitActionInvoke`** instead of a bare `Map`. Use `invoke.actionId` and `invoke.data` (merged action `data` + input values).
- **`onExecute`** now receives **`ExecuteActionInvoke`** instead of a bare `Map`. Use `invoke.verb`, `invoke.actionId`, and `invoke.data`.
- **`GenericExecuteAction.tap`** no longer takes a separate `verb` argument; read action metadata from `adaptiveMap` in custom implementations (or delegate to `ExecuteActionInvoke.fromActionMap`).
- **`onOpenUrl`** now receives **`OpenUrlActionInvoke`** (`url`, optional `actionId`) instead of a bare `String`.
- **`onOpenUrlDialog`** now receives **`OpenUrlDialogActionInvoke`** (`url`, optional `actionId`) instead of a bare `String`.
- **`onChange`** now receives **`InputChangeInvoke`** (`inputId`, `value`, `dataQuery`, `cardState`) instead of four separate parameters.
- **`AdaptiveCardsCanvas.onChange`** and **`RawAdaptiveCard.onChange`** use the same **`InputChangeInvoke`** type.
- Fixed semantic binding error when dismissing the filtered ChoiceSet modal panel (clear control separated from `InputDecoration.suffix`).

### Removed

- Unused **`onSubmit`**, **`onExecute`**, and **`onOpenUrl`** fields on **`AdaptiveCardsCanvasState`**. Use **`InheritedAdaptiveCardHandlers`** for Submit, Execute, and OpenUrl callbacks.
```

- [x] **Step 2: Verify no `## [0.10.0]` section remains**

Run: `rg '^\## \[0\.10\.0\]' packages/flutter_adaptive_cards_fs/CHANGELOG.md`
Expected: no matches

- [x] **Step 3: Commit**

```bash
git add packages/flutter_adaptive_cards_fs/CHANGELOG.md
git commit -m "$(cat <<'EOF'
Finalize flutter_adaptive_cards_fs 0.9.0 changelog for release.

EOF
)"
```

---

### Task 2: Finalize remaining CHANGELOGs for 0.9.0

**Files:**

- Modify: `packages/flutter_adaptive_charts_fs/CHANGELOG.md`
- Modify: `packages/flutter_adaptive_template_fs/CHANGELOG.md`
- Modify: `adaptive_explorer/CHANGELOG.md`
- Modify: `widgetbook/CHANGELOG.md` (already has content — verify only)

- [x] **Step 1: Update charts CHANGELOG**

Replace the `## [0.9.0]` section in `packages/flutter_adaptive_charts_fs/CHANGELOG.md`:

```markdown
## [0.9.0]

- Version alignment with `flutter_adaptive_cards_fs` 0.9.0 (`^0.9.0` dependency).
```

- [x] **Step 2: Update template CHANGELOG**

Replace the `## [0.9.0]` section in `packages/flutter_adaptive_template_fs/CHANGELOG.md`:

```markdown
## [0.9.0]

- Version alignment with `flutter_adaptive_cards_fs` 0.9.0.
```

- [x] **Step 3: Update adaptive_explorer CHANGELOG**

Replace the `## [0.9.0]` section in `adaptive_explorer/CHANGELOG.md`:

```markdown
## [0.9.0]

- Version alignment with library packages 0.9.0.
```

- [x] **Step 4: Verify widgetbook CHANGELOG**

Confirm `widgetbook/CHANGELOG.md` `## [0.9.0]` lists the ChoiceSet / valueChangedAction demos (already present). No edit required unless bullets are incomplete.

- [x] **Step 5: Commit**

```bash
git add packages/flutter_adaptive_charts_fs/CHANGELOG.md \
        packages/flutter_adaptive_template_fs/CHANGELOG.md \
        adaptive_explorer/CHANGELOG.md
git commit -m "$(cat <<'EOF'
Finalize 0.9.0 changelogs for charts, template, and explorer.

EOF
)"
```

---

## Phase B — Pre-release verification

### Task 3: Run release checklist

**Files:** (read-only verification)

- [x] **Step 1: Confirm all versions are 0.9.0**

Run from repo root:

```bash
rg '^version:' packages/*/pubspec.yaml adaptive_explorer/pubspec.yaml widgetbook/pubspec.yaml
```

Expected: five lines, all `version: 0.9.0`

- [x] **Step 2: Confirm charts dependency**

Run:

```bash
rg 'flutter_adaptive_cards_fs:' packages/flutter_adaptive_charts_fs/pubspec.yaml
```

Expected: `flutter_adaptive_cards_fs: ^0.9.0`

- [x] **Step 3: Resolve workspace**

Run from repo root:

```bash
fvm flutter pub get
```

Expected: exit 0

- [x] **Step 4: Static analysis**

Run from repo root:

```bash
fvm flutter analyze
```

Expected: no issues

- [x] **Step 5: Tests (all published packages + apps)**

Run from repo root:

```bash
fvm flutter test -r expanded
```

Working directories (match CI in `.github/workflows/test.yml`):

```bash
cd packages/flutter_adaptive_cards_fs && fvm flutter test -r expanded
cd packages/flutter_adaptive_charts_fs && fvm flutter test -r expanded
cd packages/flutter_adaptive_template_fs && fvm flutter test -r expanded
cd adaptive_explorer && fvm flutter test -r expanded
```

Expected: all pass

- [x] **Step 6: Dry-run publish (each library package)**

```bash
cd packages/flutter_adaptive_cards_fs && fvm flutter pub publish --dry-run
cd packages/flutter_adaptive_template_fs && fvm flutter pub publish --dry-run
cd packages/flutter_adaptive_charts_fs && fvm flutter pub publish --dry-run
```

Expected: no errors; version shown as `0.9.0` for each

> **Human gate:** If any dry-run reports warnings about missing LICENSE, homepage, or breaking API docs, resolve before real publish.

---

## Phase C — Tag and publish 0.9.0

> **Prerequisite:** Phase A and B complete; `main` pushed to `origin/main`.

### Task 4: Create and push git tags

- [x] **Step 1: Create tags locally**

From repo root (annotated tags recommended):

```bash
git tag -a v0.9.0 -m "Release 0.9.0"
git tag -a vflutter_adaptive_cards_fs-0.9.0 -m "flutter_adaptive_cards_fs 0.9.0"
git tag -a vflutter_adaptive_template_fs-0.9.0 -m "flutter_adaptive_template_fs 0.9.0"
git tag -a vflutter_adaptive_charts_fs-0.9.0 -m "flutter_adaptive_charts_fs 0.9.0"
```

- [x] **Step 2: Push tags**

```bash
git push origin v0.9.0 \
  vflutter_adaptive_cards_fs-0.9.0 \
  vflutter_adaptive_template_fs-0.9.0 \
  vflutter_adaptive_charts_fs-0.9.0
```

Expected: four tags on remote

---

### Task 5: Publish to pub.dev (dependency order)

> Requires pub.dev credentials (`flutter pub token add` or `PUB_CREDENTIALS`). Run **real** publish only after dry-runs pass.

**Order:** cards → template → charts (charts depends on cards being live at `^0.9.0`).

- [x] **Step 1: Publish `flutter_adaptive_cards_fs`**

```bash
cd packages/flutter_adaptive_cards_fs
fvm flutter pub publish --dry-run   # final check
fvm flutter pub publish             # interactive confirm, or --force in CI
```

- [x] **Step 2: Publish `flutter_adaptive_template_fs`**

```bash
cd packages/flutter_adaptive_template_fs
fvm flutter pub publish --dry-run
fvm flutter pub publish
```

- [x] **Step 3: Publish `flutter_adaptive_charts_fs`**

```bash
cd packages/flutter_adaptive_charts_fs
fvm flutter pub publish --dry-run
fvm flutter pub publish
```

- [x] **Step 4: Verify on pub.dev**

Confirm all three packages show version **0.9.0**:

- <https://pub.dev/packages/flutter_adaptive_cards_fs>
- <https://pub.dev/packages/flutter_adaptive_template_fs>
- <https://pub.dev/packages/flutter_adaptive_charts_fs>

> **Do not proceed to Phase D until all three are live.**

---

## Phase D — Post-release bump to 0.10.0

> Per release-engineer §6: run **only after** successful pub.dev publish.

### Task 6: Bump all `pubspec.yaml` versions to 0.10.0

**Files:**

- Modify: `packages/flutter_adaptive_cards_fs/pubspec.yaml`
- Modify: `packages/flutter_adaptive_charts_fs/pubspec.yaml`
- Modify: `packages/flutter_adaptive_template_fs/pubspec.yaml`
- Modify: `adaptive_explorer/pubspec.yaml`
- Modify: `widgetbook/pubspec.yaml`

- [x] **Step 1: Set version in all five files**

Change `version: 0.9.0` → `version: 0.10.0` in each file above.

- [x] **Step 2: Update charts in-repo pub dependency**

In `packages/flutter_adaptive_charts_fs/pubspec.yaml`:

```yaml
flutter_adaptive_cards_fs: ^0.10.0
```

---

### Task 7: Add `## [0.10.0]` placeholder sections to all CHANGELOGs

**Files:**

- Modify: all five `CHANGELOG.md` files listed in release-engineer §2

- [x] **Step 1: Insert at top of each CHANGELOG (below title/header)**

```markdown
## [0.10.0]

- no changes yet
```

Insert immediately **before** the existing `## [0.9.0]` section in:

- `packages/flutter_adaptive_cards_fs/CHANGELOG.md`
- `packages/flutter_adaptive_charts_fs/CHANGELOG.md`
- `packages/flutter_adaptive_template_fs/CHANGELOG.md`
- `adaptive_explorer/CHANGELOG.md`
- `widgetbook/CHANGELOG.md`

---

### Task 8: Verify and commit post-release bump

- [x] **Step 1: Workspace + analysis + tests**

```bash
fvm flutter pub get
fvm flutter analyze
cd packages/flutter_adaptive_cards_fs && fvm flutter test -r expanded
cd packages/flutter_adaptive_charts_fs && fvm flutter test -r expanded
cd packages/flutter_adaptive_template_fs && fvm flutter test -r expanded
```

Expected: all pass

- [x] **Step 2: Commit**

```bash
git add packages/flutter_adaptive_cards_fs/pubspec.yaml \
        packages/flutter_adaptive_charts_fs/pubspec.yaml \
        packages/flutter_adaptive_template_fs/pubspec.yaml \
        adaptive_explorer/pubspec.yaml \
        widgetbook/pubspec.yaml \
        packages/flutter_adaptive_cards_fs/CHANGELOG.md \
        packages/flutter_adaptive_charts_fs/CHANGELOG.md \
        packages/flutter_adaptive_template_fs/CHANGELOG.md \
        adaptive_explorer/CHANGELOG.md \
        widgetbook/CHANGELOG.md
git commit -m "$(cat <<'EOF'
Bump monorepo to 0.10.0 for next development cycle.

EOF
)"
```

- [x] **Step 3: Push to origin**

```bash
git push origin main
```

---

## Release cycle diagram

```text
[Current] Develop at 0.9.0 — features landed, changelogs need fix (Task 1–2)
       ↓
Verify (Task 3) → Tag v0.9.0 (Task 4) → Publish cards/template/charts (Task 5)
       ↓
Post-release bump → all versions 0.10.0, charts dep ^0.10.0, ## [0.10.0] placeholders (Task 6–8)
       ↓
[Next] Develop at 0.10.0
```

---

## Host migration note (0.9.0 breaking changes)

Document for release announcement / README if not already linked:

| Callback          | Old signature                       | New type                                                                         |
| ----------------- | ----------------------------------- | -------------------------------------------------------------------------------- |
| `onSubmit`        | `(Map data)`                        | `(SubmitActionInvoke invoke)` → `invoke.data`, `invoke.actionId`                 |
| `onExecute`       | `(Map data)`                        | `(ExecuteActionInvoke invoke)` → `invoke.verb`, `invoke.actionId`, `invoke.data` |
| `onOpenUrl`       | `(String url)`                      | `(OpenUrlActionInvoke invoke)`                                                   |
| `onOpenUrlDialog` | `(String url)`                      | `(OpenUrlDialogActionInvoke invoke)`                                             |
| `onChange`        | `(id, value, dataQuery, cardState)` | `(InputChangeInvoke invoke)`                                                     |

Reference: `docs/form-inputs.md`, `packages/flutter_adaptive_cards_fs/CHANGELOG.md`.

---

## Self-review checklist

| Requirement                                          | Task     |
| ---------------------------------------------------- | -------- |
| Fix premature 0.10.0 section in cards changelog      | Task 1   |
| All five changelogs have complete `## [0.9.0]`       | Task 1–2 |
| Pre-release verify (pub get, analyze, test, dry-run) | Task 3   |
| Tag `v0.9.0` + per-package tags                      | Task 4   |
| Publish order: cards → template → charts             | Task 5   |
| Post-release bump only after pub.dev live            | Task 6–8 |
| Charts `^0.10.0` after bump                          | Task 6   |

---

## Execution handoff

**Plan saved to `docs/superpowers/plans/2026-06-06-release-0.9.0-bump-0.10.0.plan.md`.**

**Two execution options:**

1. **Subagent-Driven (recommended)** — fresh subagent per task, review between phases (A → B → C → D with human gate before publish).

2. **Inline Execution** — run phases in this session; stop for human approval before `flutter pub publish` and before post-release bump.

**Which approach?**
