# updating these images

The simplest way to get a GitHub aligned test image is to download the artifacts zip file created from a failed build and then rename the `xxx_testImage.png` files to `xxx.png` for any of the failed tests.
