# Vendored upstream agent skills (managed by `npx skills`; keep upstream formatting)
.agents/
.claude/

# Process / decision-record artifacts — frozen, not formatted
# (mirrors the Diátaxis governance scope: published docs only)
#
# We don't have a way of knowing which plans are in progress which is when they should be formatted, so we just ignore all of them.
#docs/plans/
#docs/superpowers/
#docs/reviews/
docs/archive/

# Build / tooling output
**/build/
**/.dart_tool/
.fvm/
**/.fvm/
coverage/
**/coverage/
node_modules/

# Generated platform assets (e.g. Xcode LaunchImage READMEs)
**/Assets.xcassets/

# Generated
**/*.g.dart
